'use client'

import type { Property } from '@estatepilot/shared'
import { CalendarCheck, CheckCircle2 } from 'lucide-react'
import { FormEvent, useState } from 'react'
import { Badge, Button, Card, Input, Select, Textarea } from './ui'

export function BookingForm({property}:{property:Property}){const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api'}/appointments`,{method:'POST',headers:{'Content-Type':'application/json','x-demo-user':'demo-buyer'},body:JSON.stringify({propertyId:property.id,leadName:'Demo Buyer',date:'2026-07-18',time:'11:30',type:'physical'})})}catch{}setTimeout(()=>{setLoading(false);setDone(true)},350)}if(done)return <Card style={{padding:32,textAlign:'center'}}><CheckCircle2 size={48} color="var(--primary)"/><h2>Viewing request received</h2><p className="muted">{property.agent.name} will confirm your time shortly. A reminder will appear in your appointments.</p><Badge tone="success">Reference EP-2026-0718</Badge></Card>;return <Card style={{padding:26}}><form className="form-grid" onSubmit={submit}><div className="form-field"><label htmlFor="name">Full name</label><Input id="name" name="name" required placeholder="Your name"/></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:15}}><div className="form-field"><label htmlFor="email">Email</label><Input id="email" name="email" type="email" required placeholder="you@example.com"/></div><div className="form-field"><label htmlFor="phone">Phone</label><Input id="phone" name="phone" required placeholder="+92 3XX XXXXXXX"/></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:15}}><div className="form-field"><label htmlFor="date">Preferred date</label><Input id="date" name="date" type="date" min="2026-07-16" required/></div><div className="form-field"><label htmlFor="time">Time</label><Select id="time" name="time" required><option value="">Select</option><option>11:30 AM</option><option>2:00 PM</option><option>4:30 PM</option><option>6:00 PM</option></Select></div><div className="form-field"><label htmlFor="type">Viewing type</label><Select id="type" name="type"><option>Physical</option><option>Virtual</option></Select></div></div><div className="form-field"><label htmlFor="notes">Notes for the agent</label><Textarea id="notes" name="notes" placeholder="Questions, accessibility needs or anything else…"/></div><Button size="lg" disabled={loading}><CalendarCheck size={18}/>{loading?'Submitting…':'Request viewing'}</Button></form></Card>}
