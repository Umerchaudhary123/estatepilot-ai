'use client'

import type { Property } from '@estatepilot/shared'
import { CalendarCheck, CheckCircle2 } from 'lucide-react'
import { FormEvent, useState } from 'react'
import { Badge, Button, Card, Input, Select, Textarea } from './ui'

export function BookingForm({property}:{property:Property}){const [reference,setReference]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);try{const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api'}/appointments`,{method:'POST',headers:{'Content-Type':'application/json','x-demo-user':'demo-buyer'},body:JSON.stringify({propertyId:property.id,leadName:form.get('name'),email:form.get('email'),phone:form.get('phone'),date:form.get('date'),time:form.get('time'),type:String(form.get('type')).toLowerCase(),notes:form.get('notes')})});if(!response.ok)throw new Error('Unable to submit the viewing request. Please try again.');const result=await response.json();setReference(String(result.id).toUpperCase())}catch(cause){setError(cause instanceof Error?cause.message:'Unable to submit the request.')}finally{setLoading(false)}}if(reference)return <Card style={{padding:32,textAlign:'center'}}><CheckCircle2 size={48} color="var(--primary)"/><h2>Viewing request received</h2><p className="muted">{property.agent.name} will confirm your time shortly. A reminder will appear in your appointments.</p><Badge tone="success">Reference {reference}</Badge></Card>;return <Card style={{padding:26}}><form className="form-grid" onSubmit={submit}><div className="form-field"><label htmlFor="name">Full name</label><Input id="name" name="name" required placeholder="Your name"/></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:15}}><div className="form-field"><label htmlFor="email">Email</label><Input id="email" name="email" type="email" required placeholder="you@example.com"/></div><div className="form-field"><label htmlFor="phone">Phone</label><Input id="phone" name="phone" required minLength={10} placeholder="+92 3XX XXXXXXX"/></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:15}}><div className="form-field"><label htmlFor="date">Preferred date</label><Input id="date" name="date" type="date" min={new Date().toISOString().slice(0,10)} required/></div><div className="form-field"><label htmlFor="time">Time</label><Select id="time" name="time" required><option value="">Select</option><option>11:30 AM</option><option>2:00 PM</option><option>4:30 PM</option><option>6:00 PM</option></Select></div><div className="form-field"><label htmlFor="type">Viewing type</label><Select id="type" name="type"><option value="physical">Physical</option><option value="virtual">Virtual</option></Select></div></div><div className="form-field"><label htmlFor="notes">Notes for the agent</label><Textarea id="notes" name="notes" placeholder="Questions, accessibility needs or anything else…"/></div>{error&&<div role="alert" className="badge-danger" style={{padding:10,borderRadius:10}}>{error}</div>}<Button size="lg" disabled={loading}><CalendarCheck size={18}/>{loading?'Submitting…':'Request viewing'}</Button></form></Card>}
