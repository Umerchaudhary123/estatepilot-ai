'use client'

import { useRouter } from 'next/navigation'
import { FormEvent, useState } from 'react'
import { Button, Input } from './ui'

export function LoginForm(){const router=useRouter();const [email,setEmail]=useState('manager@demo.estatepilot.pk');const [password,setPassword]=useState('EstatePilot2026!');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!response.ok){const result=await response.json().catch(()=>({error:'Unable to sign in.'}));setError(result.error);setLoading(false);return}router.replace('/dashboard');router.refresh()}return <form className="form-grid" style={{marginTop:24}} onSubmit={submit}><div className="form-field"><label htmlFor="email">Email address</label><Input id="email" type="email" value={email} onChange={(e)=>setEmail(e.target.value)} required autoComplete="email"/></div><div className="form-field"><label htmlFor="password">Password</label><Input id="password" type="password" value={password} onChange={(e)=>setPassword(e.target.value)} required autoComplete="current-password"/></div><div style={{display:'flex',justifyContent:'space-between',fontSize:12}}><label><input type="checkbox" defaultChecked/> Remember me</label><span className="muted">Demo access</span></div>{error&&<div role="alert" style={{color:'#9b312a',background:'#fee8e5',padding:10,borderRadius:9,fontSize:12}}>{error}</div>}<Button size="lg" type="submit" disabled={loading}>{loading?'Signing in…':'Sign in to workspace'}</Button></form>}
