'use client'

import { useRouter } from 'next/navigation'
import { FormEvent, useState } from 'react'
import { getSupabaseBrowserClient } from '@/lib/supabase'
import { Button, Input } from './ui'

export function LoginForm(){const router=useRouter();const [email,setEmail]=useState('manager@demo.estatepilot.pk');const [password,setPassword]=useState('EstatePilot2026!');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const supabase=getSupabaseBrowserClient();if(!supabase){document.cookie='estatepilot-demo-session=manager; path=/; max-age=86400; samesite=lax';router.push('/dashboard');router.refresh();return}const result=await supabase.auth.signInWithPassword({email,password});if(result.error){setError(result.error.message);setLoading(false);return}router.push('/dashboard');router.refresh()}return <form className="form-grid" style={{marginTop:24}} onSubmit={submit}><div className="form-field"><label htmlFor="email">Email address</label><Input id="email" type="email" value={email} onChange={(e)=>setEmail(e.target.value)} required/></div><div className="form-field"><label htmlFor="password">Password</label><Input id="password" type="password" value={password} onChange={(e)=>setPassword(e.target.value)} required/></div><div style={{display:'flex',justifyContent:'space-between',fontSize:12}}><label><input type="checkbox" defaultChecked/> Remember me</label><a href="#" style={{color:'var(--primary)',fontWeight:650}}>Forgot password?</a></div>{error&&<div role="alert" style={{color:'#9b312a',background:'#fee8e5',padding:10,borderRadius:9,fontSize:12}}>{error}</div>}<Button size="lg" type="submit" disabled={loading}>{loading?'Signing in…':'Sign in to workspace'}</Button></form>}
