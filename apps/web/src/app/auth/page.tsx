import { Building2, CheckCircle2, Sparkles } from 'lucide-react'
import Link from 'next/link'
import { LoginForm } from '@/components/login-form'
import { Card } from '@/components/ui'

export default function AuthPage(){return <main className="auth-shell"><section className="auth-art"><Link href="/" className="brand"><span className="brand-mark" style={{background:'var(--lime)',color:'#173c2f'}}><Building2 size={19}/></span>EstatePilot AI</Link><div style={{position:'relative',zIndex:1}}><div className="eyebrow" style={{color:'var(--lime)'}}>One workspace, total clarity</div><h1 className="heading" style={{marginTop:12}}>Turn every property conversation into progress.</h1><div style={{display:'grid',gap:12,marginTop:26,color:'#c3d3cc',fontSize:14}}>{['AI-qualified leads','Unified team inbox','Viewings and follow-ups'].map((x)=><span key={x} style={{display:'flex',gap:9}}><CheckCircle2 size={17} color="var(--lime)"/>{x}</span>)}</div></div><div style={{fontSize:12,color:'#8ca59a'}}>Secure role-based access for agencies and buyers.</div></section><section className="auth-panel"><Card className="auth-card"><div className="feature-icon"><Sparkles size={20}/></div><h1 style={{fontSize:28,letterSpacing:'-.04em',marginBottom:6}}>Welcome back</h1><p className="muted" style={{fontSize:14,marginTop:0}}>Sign in to continue to your EstatePilot workspace.</p><LoginForm/><p className="muted" style={{fontSize:11,lineHeight:1.5,textAlign:'center',marginTop:18}}>Demo credentials are pre-filled. Supabase Auth activates when environment keys are configured.</p></Card></section></main>}
